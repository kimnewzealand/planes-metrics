SELECT
    YEAR,
    MONTH,
    DAY,
    DEP_TIME,
    SCHED_DEP_TIME,
    DEP_DELAY,
    ARR_TIME,
    SCHED_ARR_TIME,
    ARR_DELAY,
    CARRIER,
    FLIGHT,
    TAILNUM,
    ORIGIN,
    DEST,
    AIR_TIME,
    DISTANCE,
    HOUR,
    MINUTE,
    TIME_HOUR,
    AIRLINE,
    CALLSIGN,
    PRIMARY_HUB,
    DATE_FROM_PARTS(YEAR, MONTH, DAY) AS DEP_DATE
FROM {{ source('sky', 'flights') }}
