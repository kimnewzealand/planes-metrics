SELECT
    YEAR,
    MONTH,
    DAY,
    DATE_FROM_PARTS(YEAR,MONTH,DAY) as DEP_DATE,
    DEP_TIME,
    SCHED_DEP_TIME,
    DEP_DELAY,
    ARR_TIME,
    SCHED_ARR_TIME,
    ARR_DELAY,
    CARRIER,
    FLIGHT,
    TAILNUM,
    ORIGIN,
    DEST,
    AIR_TIME,
    DISTANCE,
    HOUR,
    MINUTE,
    TIME_HOUR,
    AIRLINE,
    CALLSIGN,
    PRIMARY_HUB
FROM {{ source('sky', 'flights') }}
